services:
  - name: eduvault
    env: python
    plan: free
    repo: https://github.com/azhaganece/eduvault.git
    branch: main
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: python -m gunicorn app:app --bind 0.0.0.0:$PORT